# AI Agent 知识地图 — 基于 final-agent 项目

> 学习策略：每个知识点按 **一句话理解 → 项目中对应的代码/配置 → 面试高频追问** 组织。
> 先读"一句话理解"快速扫盲，再读"项目对应"建立映射，最后用"面试追问"自测。

---

## 一、RAG（检索增强生成）

### 1.1 什么是 RAG

| 项目 | 内容 |
|------|------|
| **一句话理解** | 不让 LLM 凭空回答，先从知识库里检索相关材料，让 LLM 基于材料回答——相当于开卷考试。 |
| **项目对应** | 整个 final-agent 的核心就是 RAG：MinerU 整理课本（建知识库）→ ChromaDB+BM25 找答案（检索）→ DeepSeek 基于找到的内容回答（生成）。 |
| **面试追问** | ① RAG 和 Fine-tuning 什么场景用哪个？② 如果检索到的材料是错的怎么办？ |

### 1.2 Chunking（分块策略）

| 项目 | 内容 |
|------|------|
| **一句话理解** | 长文档不能整篇喂给 LLM（token 超限+检索不精确），要切成合理大小的块。 |
| **项目对应** | `chunker.py`：按 Markdown 标题层级（`##`/`###`/`####`）切分；超长段落按句号递归子分块 + overlap（重叠 1-2 句保证上下文不截断）。`min_chunk_chars=20` 防止碎片化。 |
| **面试追问** | ① chunk 大小怎么定？（项目经验：256-1024 tokens，取决于 embedding 模型和检索精度需求）② overlap 太大/太小各有什么问题？③ 如果要分中英文混合文档有什么额外要注意的？ |

### 1.3 Embedding（向量化）

| 项目 | 内容 |
|------|------|
| **一句话理解** | 把文字变成一串数字（向量），语义相近的文字向量距离近。 |
| **项目对应** | `embedder.py`：BAAI/bge-large-zh-v1.5，1024 维向量，sentence-transformers 本地加载，全局缓存（省重复加载耗时）。 |
| **面试追问** | ① BGE 和 OpenAI ada-002 怎么选？② 向量维度越大越好吗？③ 中文场景为什么选 bge-large-zh？ |

### 1.4 向量数据库

| 项目 | 内容 |
|------|------|
| **一句话理解** | 专门存向量的数据库，支持"找跟这个向量最像的 K 个"操作（ANN 近似最近邻搜索）。 |
| **项目对应** | `vector_store.py`：ChromaDB PersistentClient，cosine 距离，hnsw:space=cosine。持久化到 `data/vector_db/`。 |
| **面试追问** | ① ChromaDB vs Pinecone vs Milvus vs Weaviate？② HNSW 的 M 和 ef_search 参数怎么调？③ 为什么用 cosine 而不是 L2？ |

---

## 二、混合检索（Hybrid Search）

### 2.1 Dense vs Sparse

| 项目 | 内容 |
|------|------|
| **一句话理解** | Dense（稠密检索）= 理解意思去找（近义词也能找到）；Sparse（稀疏检索）= 找原词（"洛必达"就必须出现"洛必达"）。二者互补。 |
| **项目对应** | Dense：ChromaDB（BGE 向量）；Sparse：BM25Okapi + jieba（中文分词）。两个线程并行跑（`ThreadPoolExecutor`）后 RRF 融合。 |
| **面试追问** | ① 什么场景下只用 dense 就够了？② BM25 的 k1 和 b 参数是干嘛的？③ jieba 分词对新词（比如 ChatGPT）怎么处理？ |

### 2.2 RRF（Reciprocal Rank Fusion）

| 项目 | 内容 |
|------|------|
| **一句话理解** | 把两个排名表合并成一个：不管分数差距多大，每个结果在各自列表里的"名次"决定权重大小。 |
| **项目对应** | `hybrid_searcher.py`：公式 `score = 1/(rank + 60)`，k=60。不比较 cosine 分数和 BM25 分数——因为二者量级不同，没法直接比。 |
| **面试追问** | ① k 越大/越小分别适合什么场景？② RRF 跟加权平均比优势在哪？③ 除了 RRF 还有哪些融合方法（如 DBSF、CC）？ |

### 2.3 Reranker（精排）

| 项目 | 内容 |
|------|------|
| **一句话理解** | RRF/向量检索做"海选"（快速筛出 top 几十），Reranker 做"终选"（仔细对比每个候选跟问题的匹配度）。 |
| **项目对应** | `reranker.py`：BAAI/bge-reranker-v2-m3，cross-encoder（问题+chunk一起输入计算匹配分），比单纯向量检索准但慢。两阶段策略：RRF 粗筛→reranker 精排。 |
| **面试追问** | ① cross-encoder 和 bi-encoder 的区别？② 为什么 reranker 不直接用来检索？③ 如果 top 60 都跟问题不相关，reranker 能救吗？ |

---

## 三、AI Agent 核心概念

### 3.1 Tool-Use（函数调用）

| 项目 | 内容 |
|------|------|
| **一句话理解** | LLM 不只能聊天——给它定义好一批"工具"（函数），它能自主决定用哪个、怎么用、以及什么时候用。 |
| **项目对应** | 目前是**被动 RAG**（用户问→检索→回答）。但要升级为 Agent，可以定义工具：`retrieve_knowledge(query)`, `generate_quiz(topic)`, `summarize(chunk_ids)`, `track_progress()` → LLM 自主编排。第 7 题的 answer.md 有详细设计。 |
| **面试追问** | ① Tool-Use 的 function calling 格式（OpenAI 式）怎么写？② 工具返回出错 LLM 怎么感知？③ 工具链太长导致超出上下文怎么办？ |

### 3.2 Agent 记忆系统

| 项目 | 内容 |
|------|------|
| **一句话理解** | Agent 需要三种记忆：工作记忆（当前对话）、情景记忆（之前会话的摘要）、语义记忆（积累的知识点掌握度）。 |
| **项目对应** | 当前有**工作记忆**（`AppState.conversations`，多对话切换）。缺少**情景记忆**（跨会话的摘要）和**语义记忆**（每个知识点的掌握程度）。第 9 题 answer.md 设计了完整方案。 |
| **面试追问** | ① 长期记忆的存储格式（JSON/向量库/图数据库）怎么选？② 记忆检索失败的优雅降级策略？③ 如何防止记忆无限膨胀？ |

### 3.3 Planning（任务规划）

| 项目 | 内容 |
|------|------|
| **一句话理解** | Agent 把一个模糊目标（"帮我复习微积分"）自动拆成可执行的步骤序列。 |
| **项目对应** | 目前没有（用户的任务就是问问题）。要做主动学习 Agent 的话：Plan→Execute→Observe→Reflect 循环。第 7 题 answer.md 有 Orchestrator LLM 的设计。 |
| **面试追问** | ① ReAct vs Plan-and-Execute 两种模式区别？② 规划失败（LLM 产生无效计划）怎么检测和恢复？③ 子任务之间的依赖关系如何管理？ |

---

## 四、防幻觉（Hallucination Mitigation）

### 4.1 基于引用的防幻觉

| 项目 | 内容 |
|------|------|
| **一句话理解** | 强制 LLM 在答案里标注每一句话来自哪个 chunk，然后逐句校验引用是否靠谱。 |
| **项目对应** | `guard.py`：用 bge-reranker 逐句计算答案句子和引用 chunk 的 cross-encoder 相似度，低于 0.3 标记 ⚠️。 |
| **面试追问** | ① 阈值 0.3 怎么来的？你是怎么调出来的？② 如果 LLM 不按格式写引用怎么办？③ 引用检查和语义相似度检查各自解决了什么问题？ |

### 4.2 其他防幻觉手段

| 项目 | 内容 |
|------|------|
| **一句话理解** | 除了引用校验，还有输入侧约束、提示词工程、模型选择、输出验证等多层防线。 |
| **项目对应** | 项目用了三层：① System prompt 约束（"仅基于提供材料回答"）；② 检索增强（给足上下文减少模型编造动机）；③ L2 Guard（输出后验证）。 |
| **面试追问** | ① 这三层各自拦截了什么类型的幻觉？② 防幻觉和创造性之间如何平衡？③ 还有哪些防幻觉技术（Self-Reflection、Self-RAG、KG 约束）？ |

---

## 五、LLM 工程实践

### 5.1 API 调用模式

| 项目 | 内容 |
|------|------|
| **一句话理解** | 跟 LLM 通信的方式：同步（等全部返回）、流式（一个字一个字出）、函数调用（返回结构化数据）。 |
| **项目对应** | `llm_client.py`：OpenAI 兼容客户端，支持 stream 模式（Streamlit UI 逐 token 显示）。`config.yaml` 配 base_url 切换不同模型（DeepSeek / 火山方舟）。 |
| **面试追问** | ① 流式输出下怎么做引用标注（需要等到完整句子才能做 Guard）？② API 调用的重试策略和退避算法？③ 怎么估算 token 消耗和控制成本？ |

### 5.2 Prompt Engineering

| 项目 | 内容 |
|------|------|
| **一句话理解** | 写好给 LLM 的"说明书"：你是谁、你要做什么、输入什么、输出格式要求、注意事项。 |
| **项目对应** | `prompts.py`：三个模板——QA（逐句引用检测）、Review（结构化复习摘要）、Exam（模拟题）。每个模板都明确要求输出格式和引用规则。 |
| **面试追问** | ① System prompt vs User prompt 的区别和最佳实践？② few-shot 示例对模型输出的影响？③ prompt 注入怎么防？ |

### 5.3 容错设计

| 项目 | 内容 |
|------|------|
| **一句话理解** | LLM 调用一定会出错（网络超时、格式错误、内容被拒），系统必须有策略来处理。 |
| **项目对应** | `interviewer-note.md` 记录了多次 API 相关 Bug。第 8 题 answer.md 设计了三级容错：轻试→格式强调→简化回退。 |
| **面试追问** | ① 指数退避的公式？② 怎么区分"临时错误"和"永久错误"？③ 部分成功怎么优雅返回用户？ |

---

## 六、系统架构设计

### 6.1 模块化 Pipeline

| 项目 | 内容 |
|------|------|
| **一句话理解** | 一个大任务拆成多个独立模块，每个模块只做一件事，模块之间通过数据契约（Schema）通信。 |
| **项目对应** | `schemas.py`：`Chunk` / `ScoredChunk` / `GeneratedAnswer` / `HallucinationFlag` / `VerifiedAnswer` 五个 Pydantic 模型，Ingestion→Knowledge→Retrieval→Generation→UI 全部基于这些数据契约。 |
| **面试追问** | ① Pipeline 和 Agent（循环决策）架构的区别？② 数据契约变更时如何保证各模块兼容？③ 为什么选择 Pydantic 而不是 dataclass？ |

### 6.2 子进程隔离

| 项目 | 内容 |
|------|------|
| **一句话理解** | 把"危险"或"重型"的任务扔到独立进程里跑，主进程崩溃了也不影响其他部分。 |
| **项目对应** | `pdf_parser.py`：MinerU CLI 子进程，隔离 torch 依赖，不污染 sentence-transformers 的 Python 环境。 |
| **面试追问** | ① 子进程 vs 进程内线程 vs 独立微服务的优劣？② 子进程崩溃后怎么自动恢复？③ 进程间通信选文件还是消息队列？ |

### 6.3 配置管理

| 项目 | 内容 |
|------|------|
| **一句话理解** | 系统行为不应该硬编码在代码里，而是写在配置文件里，改了配置就等于改了行为。 |
| **项目对应** | `config.yaml` + `settings.py`：Pydantic Settings 模式封装，YAML 中 `${DEEPSEEK_API_KEY:-}` 环境变量占位，`_sanitize_llm()` 防御 None。 |
| **面试追问** | ① 配置和代码分离到什么粒度？② 敏感信息（API Key）如何在配置系统中安全处理？③ 多环境（dev/staging/prod）配置策略？ |

---

## 七、面试最可能被问的实战场景

### 场景 1: "给我设计一个 RAG 系统"

**面试官期待的框架：**
1. **文档加载**：PDF→Markdown→Chunk（选什么 parser，为什么）
2. **索引构建**：Embedding（选什么模型）+ 向量库（选什么 DB）
3. **检索策略**：Dense + Sparse 混合 → RRF 融合 → Reranker 精排
4. **生成**：Prompt 模板 + 引用注入 + 流式输出
5. **防幻觉**：Source citation + 语义校验 + 阈值调优
6. **评测**：Retrieval recall/NDCG + Generation 人工/LLM-as-Judge

**这个项目里对应哪些代码就全在上面了。**

### 场景 2: "怎么把 RAG 系统升级成 Agent？"

**关键区别——从被动到主动：**
| RAG | Agent |
|-----|-------|
| 用户问一句答一句 | Agent 能主动追问、规划、执行工具 |
| 只检索文本 | 可调用 API、运行代码、查数据库 |
| 一次性回答 | 多步推理、工具结果反馈、自我修正 |
| 无记忆 | 长期/短期/语义记忆 |

### 场景 3: "线上 RAG 效果不好，怎么排查？"

**排查路线图：**
```
效果不好
  │
  ├── 1. 查询质量   → query_expander 是否把语义弄偏了？
  ├── 2. 检索召回   → 真正的答案在不在 top 50 里？
  │                   不在→检查 chunk 质量 / embedding 模型
  ├── 3. 精排质量   → 正确的答案在 top 50 但被 reranker 排后面？
  │                   在→检查 reranker 阈值
  ├── 4. 生成质量   → 正确的上下文给了 LLM，但答错了？
  │                   在→检查 prompt / 模型能力
  └── 5. 防幻觉误报 → 正确的引用被 Guard 标记了？
                       在→检查防幻觉阈值
```

---

## 八、学习建议

### 按这个顺序学

1. **先吃透 RAG 基础**（上面的"一、二"）——这是所有 AI Agent 的"数据底座"
2. **理解防幻觉机制**（四）——面试必问 RAG 的脆弱性
3. **理解 LLM 工程**（五）——API 调用、容错、prompt 是日常工作
4. **攻 Agent 核心**（三）——Tool-Use + 记忆 + 规划，这是 AI Agent 面试的核心区
5. **看架构设计**（六）——拔高用的，区分"会用框架"和"能搭框架"

### 怎么用这个知识地图

1. 每个知识点看"一句话理解"→ 1 分钟
2. 看"项目对应"找到项目里的真实代码 → 5 分钟（自己打开文件看看）
3. 对着"面试追问"自测，答不上来的说明这个点还不够透 → 回去补
4. 最后自己在白板上画一遍项目的完整架构图（不看文档），画到哪卡住就说明哪块还没真正理解

---

> 有任何知识点想深入展开，或想让我针对某个点再出一道"拷问题"，随时说。
